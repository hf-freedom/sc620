package com.production.enums;

public enum WorkOrderStatus {
    PENDING,
    READY,
    SCHEDULED,
    IN_PRODUCTION,
    PAUSED,
    COMPLETED,
    QUALITY_INSPECTION,
    QUALITY_PASSED,
    QUALITY_FAILED,
    REWORK,
    CANCELLED
}
